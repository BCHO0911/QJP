# 存储器实验 — 代码讲解

## 一、实验做什么

用 VHDL 写一个控制器，通过 FPGA 芯片上的引脚驱动一块 **SRAM（静态随机存取存储器）**，实现：

1. **写 RAM**：拨动实验板上的拨码开关，把数据写入 RAM 的指定单元
2. **读 RAM**：逐个从 RAM 中读出刚才写入的数据，在 LED 上显示

整个过程由一个**状态机**控制。

---

## 二、整体结构（3 个核心模块）

代码虽然写在一个文件里，但逻辑上分为 **3 个部分**，每个部分对应一个 `process`：

```
┌─────────────────────────────────────────────────┐
│                   节拍发生器                      │
│  tick_gen: 50MHz 时钟 → 约 10ms 节拍信号          │
└───────────────────┬─────────────────────────────┘
                    │ tick_10ms
┌───────────────────▼─────────────────────────────┐
│                   主状态机                        │
│  main_proc: 根据节拍在 7 个状态间切换              │
│           管理读写控制和内存读写周期               │
└───────────────────┬─────────────────────────────┘
                    │ 当前状态 + 内部寄存器
┌───────────────────▼─────────────────────────────┐
│                 组合输出逻辑                       │
│  comb_out: 根据状态产生 RAM 控制信号和 LED 显示    │
└─────────────────────────────────────────────────┘
```

---

## 三、端口说明（entity 部分）

| 端口名 | 方向 | 位宽 | 含义 |
|--------|------|------|------|
| `CLK` | 输入 | 1 | 系统时钟，实验板提供 50MHz |
| `RST` | 输入 | 1 | 复位信号，**低电平有效**（按下复位键 = '0'） |
| `ctrl_r` | 输入 | 1 | 控制信号，'0' 写模式，'1' 读模式 |
| `Input_data` | 输入 | 16 位 | 拨码开关输入的值（既当地址又当数据） |
| `LIGHT` | 输出 | 16 位 | 16 个 LED 灯，用来显示地址和数据 |
| `ADDR` | 输出 | 16 位 | RAM 地址线 |
| `DATA` | 双向 | 16 位 | RAM 数据线（**双向**！写时 FPGA 驱动，读时 SRAM 驱动） |
| `RAM1_EN` | 输出 | 1 | RAM 片选使能，= '1' 时才选中这块 RAM |
| `RAM1_WE` | 输出 | 1 | RAM 写使能，= '1' 时执行写入 |
| `RAM1_OE` | 输出 | 1 | RAM 读使能，= '1' 时执行读取 |
| `DBC` | 输出 | 1 | 数码管位选，本实验未使用 |

### 关键问题：为什么 DATA 是 inout（双向）？

> SRAM 芯片的数据线是同一组引脚，写入时 FPGA 把数据送出去，读取时 SRAM 把数据送回来。
> 所以这个端口必须是双向的。不写入时设为**高阻态 'Z'**，不干扰 SRAM 的读取操作。

---

## 四、三个核心模块详解

### 4.1 节拍发生器（tick_gen）

**作用**：把 50MHz（20ns）的快时钟变成一个约 10ms 的节拍信号。

**为什么需要**：如果不分频，状态机每 20ns 就跳一次，LED 闪得太快肉眼根本看不到。10ms 的节奏适合人眼观察。

**原理**：
```
50,000,000 Hz ÷ 500,000 = 100 Hz ≈ 10ms
```

计数器从 0 数到 `TICK_DIV - 1`（499999），每到顶就产生一个 `tick_10ms = '1'` 脉冲，然后清零重新数。

> `TICK_DIV` 是 generic 参数，默认 500000（硬件用）。仿真时改成 50，否则仿真跑几百万个周期太慢。

### 4.2 主状态机（main_proc）—— 最重要的部分

**状态定义**（7 个状态）：

```
S_IDLE        → 空闲，等待用户选择读写
S_WR_SETUP    → 写准备：从开关锁存地址和数据
S_WR_EXEC     → 写执行：输出地址和数据到 RAM 端口
S_WR_DISPLAY  → 写显示：写入完成后 LED 亮一下展示
S_RD_SETUP    → 读准备：设置要读的地址
S_RD_EXEC     → 读执行：触发 RAM 读取，锁存读出的数据
S_RD_DISPLAY  → 读显示：LED 显示读出的数据
```

**状态转移图**：

```
                  ctrl_r='0'
    ┌───────┐ ┌─────────► S_IDLE ────────────┐
    │       │ │                               │
    ▼       │ │                               │
┌────────┐  │ │                          ctrl_r='1'
│ WR_    │──┘ │                               │
│ SETUP  │    │                               ▼
└───┬────┘    │                           ┌────────┐
    │         │                           │ RD_    │
    ▼         │                           │ SETUP  │
┌────────┐    │                           └───┬────┘
│ WR_    │    │                               │
│ EXEC   │    │                               ▼
└───┬────┘    │                           ┌────────┐
    │         │                           │ RD_    │
    ▼         │                           │ EXEC   │
┌────────┐    │                           └───┬────┘
│ WR_    │────┘                               │
│DISPLAY │        ┌───────────────────────────┘
└────────┘        │
    ┌─────────────┤
    │ count_reg   │
    │ >= 0xA      ▼
    ▼          ┌──────────┐
 S_IDLE ◄───── │ RD_      │
               │ DISPLAY  │
               └────┬─────┘
                    │
                    ▼ (循环读下一个)
               ┌────────┐
               │ RD_    │
               │ SETUP  │
               └────────┘
```

**写模式完整流程**：
1. `S_IDLE`：`ctrl_r='0'`，锁存开关数据到 `addr_reg` 和 `data_reg`
2. `S_WR_SETUP`：准备一个节拍
3. `S_WR_EXEC`：数据送到 RAM 端口，WE 拉高，SRAM 完成写入
4. `S_WR_DISPLAY`：LED 显示刚写入的值，回到 `S_IDLE` 等待下一组数据

**读模式完整流程**：
1. `S_IDLE`：`ctrl_r='1'`，地址归零，计数器归零
2. `S_RD_SETUP`：检查计数器是否 >= 10，满了就回到空闲
3. `S_RD_EXEC`：OE 拉高，SRAM 把数据送到总线，FPGA 锁存到 `read_data`，地址+1
4. `S_RD_DISPLAY`：LED 显示读出的值，回到 `S_RD_SETUP` 继续下一个

### 4.3 组合输出逻辑（comb_out）

**作用**：根据当前状态，决定输出什么信号。

它是一个**组合逻辑**（process 敏感列表里全是信号，没有 CLK），所以状态一变输出**立刻**跟着变。

每个状态下设置不同的控制信号：

| 状态 | RAM1_EN | RAM1_WE | RAM1_OE | DATA 驱动方式 |
|------|---------|---------|---------|--------------|
| S_IDLE | 0 | 0 | 0 | 高阻 'Z' |
| S_WR_SETUP | 1 | 0 | 0 | FPGA 输出 data_reg |
| S_WR_EXEC | 1 | **1** | 0 | FPGA 输出 data_reg |
| S_WR_DISPLAY | 1 | 0 | 0 | FPGA 输出 data_reg |
| S_RD_SETUP | 1 | 0 | **1** | 高阻 'Z'（SRAM 驱动） |
| S_RD_EXEC | 1 | 0 | **1** | 高阻 'Z'（SRAM 驱动） |
| S_RD_DISPLAY | 1 | 0 | **1** | 高阻 'Z'（SRAM 驱动） |

---

## 五、SRAM 读写时序特点（对应思考题）

### 写时序特点
1. **地址先建立**：地址线先稳定输出
2. **数据跟上**：数据线送到
3. **WE 拉低（或拉高，取决于具体 SRAM 芯片）**：触发写入
4. 写入是异步的，不需要时钟，只要控制信号到位就完成

### 读时序特点
1. **地址先建立**：地址线稳定
2. **OE 拉高**：触发读取
3. **SRAM 内部有延迟**（通常十几纳秒），数据才出现在总线上
4. 读也是异步的，不需要时钟
5. **本设计在读 SETUP 阶段就拉高 OE**，给 SRAM 留出稳定时间，EXEC 阶段才锁存数据

---

## 六、数据流向示意

### 写操作的数据流
```
拨码开关 → Input_data → addr_reg / data_reg
         → ADDR 引脚  → SRAM 地址端
         → DATA 引脚  → SRAM 数据端
         → RAM1_WE='1' → 数据写入 SRAM
         → LIGHT 显示（高8位数据，低8位地址）
```

### 读操作的数据流
```
addr_reg → ADDR 引脚 → SRAM 地址端
RAM1_OE='1' → SRAM 读取
SRAM → DATA 引脚 → read_data 锁存
         → LIGHT 显示（高8位读出数据，低8位地址）
```

---

## 七、管脚约束（UCF 文件）

UCF 文件的作用是把 VHDL 代码里的信号名绑定到 FPGA 芯片的真实物理引脚上。

- 拨码开关 16 个引脚 → J6~T7
- LED 16 个引脚 → D10~A14
- RAM 地址线 16 个引脚 → C17~H15
- RAM 数据线 16 个引脚 → J12~M14
- RAM 控制信号 3 个引脚 → M15(M_EN) M16(M_OE) M18(M_WE)
- 时钟 → U9
- 复位 → U11
- 控制信号 → U10

这些管脚编号由实验板硬件决定，不能随意更改。

---

## 八、老师可能会问的问题

### Q1：状态机是 Mealy 还是 Moore？
答：这是一个**两段式状态机**（Moore 型为主）。状态转移在时序 process 中完成，输出在组合 process 中根据状态产生。输出只与当前状态有关，不直接依赖输入，符合 Moore 的特点。

### Q2：为什么用两个 process 而不是一个？
答：把状态转移（时序逻辑）和输出生成（组合逻辑）分开，是**两段式写法**，代码更清晰，时序也更好分析。如果用一段式，所有逻辑都写在时钟沿里，读起来不容易区分哪些是时序哪些是组合。

### Q3：DATA 引脚在高阻态时，谁来驱动它？
答：读模式下，FPGA 的 DATA 输出设为 `'Z'`（高阻），此时外部的 **SRAM 芯片** 通过 OE 信号驱动 DATA 总线，把读出的数据送回来。

### Q4：节拍频率怎么算的？
答：`50,000,000 ÷ 500,000 = 100 Hz`，周期 = 1/100 = **10ms**。

### Q5：为什么要连续读 10 个单元？
答：实验任务书要求连续写 10 个数再读出验证。代码里用 `count_reg` 计数，到 `x"000A"`（10）就停止读取，回到空闲。

### Q6：SRAM 和 DRAM 有什么区别？
答：SRAM（静态 RAM）不需要刷新，只要通电数据就不丢，速度快但集成度低；DRAM（动态 RAM）需要定期刷新，集成度高但速度慢一些。FPGA 开发板上配的通常是 SRAM。

### Q7：如果不用分频，直接用 50MHz 时钟驱动状态机会怎样？
答：状态机每 20ns 就跳一次，LED 切换速度远超人眼识别能力（人眼约 10~16ms 刷新率），看到的是一片模糊。分频到 10ms 后，每次状态停留约 10ms，肉眼就能看到变化。

### Q8：复位信号为什么是低电平有效？
答：实验板上的复位按键按下时接地，产生低电平。这是硬件设计决定的，代码要配合硬件。
